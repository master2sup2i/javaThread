package PremierTest;

public class testing 
{ 
	public static int fact (int i)
	{ 
		int j, f=1 ; 
		for (j=1; j <=i; j++)
		{ f = f*j; }
		return (f);
	}
	
	public static boolean parite (int i)
	{
		if (i%2==0)
		{
			return true;
		}
		else {return false;}
			
		
	}
	
}
